//Q 6.Write a program to check whether a given character is uppercase or lowercase.

#include<stdio.h>
void main()
{
	char ch = 'a';
	
	if(ch >= 'a' && ch <= 'z');
	{
		printf("Lower case");
	}
	else
	{
		printf("Upper case");
	}
}