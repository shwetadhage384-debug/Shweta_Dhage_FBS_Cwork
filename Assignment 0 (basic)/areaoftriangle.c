//Q 9.Write a C program to input the base and height of a triangle and calculate its area.

#include<stdio.h>
void main()
{
	float base = 10, height = 5, area;
	area = 0.5 * base * height;
	
	printf("Area of Triangle = %f", area);
}